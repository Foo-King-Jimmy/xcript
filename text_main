
int
main( int argc, char **argv ) {

  $errno  = 0;            // -
  $argc   = --argc;
  $argv   = ++argv;
  $mlistc =          0;   // -
  $mlistt = (size_t) 0;   // -
  $mlistp = NULL;         // -
  getpid_();              // $pid = getpid(); $ppid = getppid();

